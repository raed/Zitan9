package simulation.ex02
import simulation.ex01


concept Student includes Person {

    matrikelnummer : int

    //  Now extends Student with the ConceptAttribute: belegtVorlesung
    //  conceptAttribute : ID ':' ('domain' '=' ID)? '->' ('range' '=' ID) ('is' property)? ;

    belegtVorlesung : (domain = Studnet) -> (range = Vorlesung) | is non-functional

}